{% extends 'project_frame.html.twig' %}

{% block title %}Planning poker de l'issue {{issue.number}}{% endblock %}

{% block content %}
    <div class="container">
        <div class="card">
            <div class="card-body">
                <h3>Evaluez la difficulté que devrais prendre cette issue</h3>
                <p>{{issue.description}}</p>
                {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ form_row(form.value) }}
                <button type="submit" class="btn btn-primary">Valider</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
