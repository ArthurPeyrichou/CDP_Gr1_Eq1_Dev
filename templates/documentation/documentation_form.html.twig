{% extends 'project_frame.html.twig' %}

{% block title %}Création d'une documentation{% endblock %}

{% block content %}

    <div class="container">
        <div class="card">
            <div class="card-body">
                <h3>Créer une documentation:</h3>
                {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ form_row(form.name) }}
                {{ form_row(form.description) }}
                {{ form_row(form.link) }}
                <button type="submit" class="btn btn-primary">Créer la documentation</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
